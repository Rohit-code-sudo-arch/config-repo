global:
  message: Welcome to global Microservices Environment
logging:
  level:
    root: INFO
management:
  endpoints:
    web: health, info, refresh
  endpoint:
    health:
      show-details: always
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    
